# 党建工作台 - 产品需求文档 (PRD)

## 1. 产品概述

### 1.1 产品名称
党建工作台

### 1.2 产品定位
面向基层党建工作人员的内部管理工具，实现工作内容的数字化归档、智能分类、统计分析与信息稿自动生成，提升党建工作效率与规范化水平。

### 1.3 目标用户
- **工作人员**：上传工作内容与图片，完成日常归档
- **管理员**：管理分类标签，浏览、筛选、统计工作内容，生成总结报告与信息稿

### 1.4 产品目标
- 实现工作内容的集中化、结构化存储
- 借助 AI 能力减少人工分类与文字撰写工作量
- 提供直观的数据统计视图，辅助工作汇报
- 一键生成可编辑 Word 信息稿，提高信息报送效率

---

## 2. 功能需求

### 2.1 工作流1：工作内容归档

#### 2.1a 工作条目上传
| 项目 | 描述 |
|------|------|
| **功能描述** | 工作人员逐条录入工作内容，每条工作内容独立关联自己的图片 |
| **数据模型** | 一个"批次"= 同一工作日期下的一组工作条目；每条"工作条目"= 一段文字描述 + 多张图片（一对多） |
| **输入** | 工作发生日期、N 条工作条目（每条含：文字描述 + 0~20 张图片） |
| **输入约束** | 每条文字描述长度 10~2000 字；图片格式 jpg/png/webp，单张 ≤10MB；每批次最多 50 条工作条目 |
| **输出** | 批次创建成功，自动触发 AI 分类，跳转至分类结果页 |
| **前置条件** | 已登录系统（工作人员或管理员角色） |
| **交互流程** | 1. 选择工作发生日期（日历组件，默认当天）<br>2. 点击"添加工作条目"，展开一个条目表单（工作描述文本框 + 独立图片上传区）<br>3. 可反复点击"添加工作条目"追加更多条目（每个条目独立管理自己的一组图片）<br>4. 每个条目内的图片支持拖拽/点击上传、缩略图预览、单张删除<br>5. 支持删除某个条目或调整条目顺序<br>6. 点击"提交归档"一次性保存整个批次<br>7. 提交前客户端校验必填项 |
| **异常处理** | - 文件格式不支持/超限时 red toast 提示<br>- 提交网络失败时保留已填写内容（localStorage 兜底）<br>- 空条目（无文字且无图片）自动跳过，不记录 |

#### 2.1b AI 智能分类
| 项目 | 描述 |
|------|------|
| **功能描述** | 批次提交后，服务端逐条调用 Deepseek API 对每条工作内容的描述文字进行归类，打上分类标签后存入数据库 |
| **输入** | 单条工作内容描述文字 + 当前系统可用的分类标签列表 |
| **输出** | 每条工作内容自动标注一个分类标签（如"民生实事"、"环境整治"、"安全生产"等）。若 AI 返回的分类不在已定义列表中，默认回退为"未分类"，并记录原始返回值供管理员审核 |
| **system prompt 设计** | "你是一名党建工作内容分类助手。请阅读以下工作内容描述，从提供的分类标签列表中选出最匹配的一个标签，仅返回标签名称，不要添加任何解释。分类标签列表：{categories}。工作内容：{description}" |
| **分类时机** | 同步调用（等待 AI 返回后整个批次才算提交完成），前端显示"正在智能分类..."加载状态 |
| **默认分类** | 民生实事、环境整治、安全生产 |
| **分类管理** | 管理员可通过"分类管理"页新增/编辑/删除自定义分类标签；删除标签前校验该标签下是否仍有工作记录，如有则提示先迁移 |
| **手动更正** | 管理员在浏览页可手动更改任意工作条目的分类标签（下拉选择器） |
| **AI 成本控制** | 单条分类请求仅发送文字描述的前 300 字；每次请求 max_tokens 设为 20；分类 prompt 固定缓存，不计入重复传输 |
| **异常处理** | - API 调用失败/超时(30秒)时标记为"未分类"，前端提示"N条分类失败，可稍后在浏览页手动归类"<br>- API 返回非预期格式时同样降级为"未分类" |

#### 2.1c 内容浏览与筛选
| 项目 | 描述 |
|------|------|
| **功能描述** | 管理员/工作人员通过多维度筛选条件查看工作内容记录，支持编辑与删除 |
| **筛选维度** | - 关键词搜索：对工作描述做全文模糊匹配（SQLite LIKE）<br>- 日历选择单日：展示当天所有工作条目<br>- 分类筛选：按标签类型过滤（下拉多选，默认"全部"）<br>- 时间段筛选：选择起止日期，展示区间内的工作条目<br>- 组合筛选：以上条件 AND 叠加 |
| **展示形式** | 卡片列表，每张卡片：分类标签色块、工作日期、内容摘要（前80字省略号）、图片缩略图网格（第一张图片在前）<br>分页：后端 offset/limit，前端 Ant Design Pagination，每页默认 12 条 |
| **缩略图策略** | 原图上传时服务端同步生成 300px 宽度缩略图存于 `uploads/thumbnails/`，卡片列表使用缩略图，点击详情切换原图加载 |
| **卡片交互** | - 点击卡片 / "查看详情"按钮 → 弹出抽屉(Drawer)：完整文字、图片大图轮播（支持左右切换）、工作日期、分类标签<br>- 详情抽屉内提供"编辑"按钮，进入编辑模式（允许修改描述文字、切换分类标签、增删图片、修改工作日期）<br>- "删除"按钮需二次确认弹窗（"确定删除该工作记录及其关联图片？此操作不可恢复。"），确认后删除数据库记录及磁盘图片文件 |
| **权限** | 工作人员仅可编辑/删除自己上传的记录；管理员可编辑/删除所有记录 |
| **空状态** | 筛选无结果时展示 Empty 组件 + 提示"暂无匹配的工作内容记录" |
| **异常处理** | - 图片缩略图/原图加载失败时显示 Ant Design Image fallback 占位<br>- 分页查询超时(>2s)时提示"查询超时，请缩小筛选范围" |

#### 2.1c-附录：编辑功能细节
| 操作 | 行为 |
|------|------|
| 修改描述文字 | 可编辑，保存后不重新触发 AI 分类（保持现有分类标签） |
| 修改分类标签 | 下拉选择器从分类表中读取，选择后即时保存（无需额外确认） |
| 修改工作日期 | 日期组件，修改后更新 work_date |
| 增加图片 | 在现有图片网格末尾点击"+"上传新图 |
| 删除图片 | 悬停出现删除按钮，点击后即时删除（磁盘文件同步删除） |
| "重新分类"按钮 | 调用 Deepseek API 重新对该条进行分类（覆盖当前标签） |

#### 2.1d 数据统计与 AI 总结
| 项目 | 描述 |
|------|------|
| **功能描述** | 按时间段与分类维度展示工作频次统计，支持 AI 自动生成工作总结文字，支持导出统计报表 |
| **统计功能** | - 选择时间段（起止日期）<br>- 选择分类类型（下拉多选，默认"全部"）<br>- 自动统计各分类在指定时间内的工作次数<br>- 以图表可视化展示 |
| **图表类型** | - 柱状图：各分类工作次数对比<br>- 饼图：各分类工作量占比<br>- 时间折线图：选定时段内每日工作数量趋势<br>图表使用 @ant-design/charts，支持交互悬浮显示具体数值 |
| **AI 总结输入** | 将统计数值 + 每条工作内容的前 200 字摘要发送至 Deepseek API（全量记录的完整原文不发送，控制 Token 消耗） |
| **AI 总结 prompt** | "你是一名党建工作汇报秘书。请根据以下信息撰写一段正式的工作汇总报告，语气客观务实、适合向领导汇报。数据为：在 {startDate} 至 {endDate} 期间，各类工作统计如下：{stats}。以下为各工作的摘要内容：{summaries}。要求200至500字。" |
| **AI 总结输出** | AI 生成的总结文字显示在页面文本区，支持一键复制到剪贴板 |
| **成本控制** | - 总结生成：每条工作内容仅发送前200字，最多取最近50条；max_tokens 设为 800<br>- 统计查询无数量限制，AI 总结有截断限制（提示：当前时间段内记录超过50条，仅对最近50条进行总结） |
| **统计导出** | 点击"导出报表(Excel)"按钮 → 服务端生成 Excel 文件（使用 exceljs 库），包含两页：<br>Sheet1 "分类统计"：分类名称、次数、占比<br>Sheet2 "明细清单"：日期、分类、描述摘要<br>文件名格式：`统计报表_{startDate}_{endDate}.xlsx` |
| **输出** | AI 生成的总结文字显示在页面文本区，可复制 |
| **异常处理** | - API 调用失败时提示"AI 总结生成失败，请重试"，不影响统计图表正常展示<br>- 统计区间内无数据时显示 Empty 状态"所选时间段内暂无工作记录"<br>- Excel 生成失败时提示重试 |

#### 2.1e 信息稿生成（Word导出）
| 项目 | 描述 |
|------|------|
| **功能描述** | 选择指定的工作内容记录，利用 AI 对原文进行扩写，并结合关联图片生成可编辑的 Word 文档 |
| **操作流程** | 1. 在浏览页面勾选需要生成信息稿的工作条目（可多选，最多选择 10 条以控制 Token）<br>2. 点击"生成信息稿"按钮，跳转至信息稿生成页面<br>3. 系统逐条调用 Deepseek API 对每条文字进行扩写（润色为正式信息稿风格）<br>4. 每条扩写结果展示在独立可编辑文本框内（支持用户手动微调）<br>5. 可上下拖动调整条目顺序<br>6. 点击"导出Word文档"<br>7. 系统生成 .docx 文件（使用 docx 库），触发浏览器下载 |
| **扩写 prompt** | "你是一名党政信息稿撰写员。请将以下工作记录扩写为一段正式的信息稿段落，语气庄重务实、用词规范，适当丰富背景和成效描述，但不得虚构事实。控制在 200 至 400 字。工作记录：{description}" |
| **AI 成本控制** | - 最多选择 10 条工作内容进行扩写，超过时前端提示<br>- 每条扩写 max_tokens 设为 600<br>- 若部分条目扩写失败（API 异常），回退使用原文，不影响整体 Word 生成<br>- 扩写并发请求（同时发送多条），总等待时间 = 单次请求时间 |
| **Word 文档格式** | - 标题：幼圆 18pt 加粗居中 "党建工作信息稿"<br>- 副标题：宋体 14pt 居中 日期<br>- 正文每条：小标题（分类标签）宋体 14pt 加粗 + 正文宋体 12pt，1.5 倍行距<br>- 图片：每条正文后插入关联图片（宽度12cm，居中对齐，等比例缩放）<br>- 页脚：楷体 10pt "——来源：党建工作台" |
| **异常处理** | - 扩写 API 全部失败时直接使用原文生成 Word（不丢失数据）<br>- 关联图片不存在时跳过该图片位置，文字照常输出<br>- Word 生成过程中前端显示"正在生成文档..." Spin 加载状态<br>- 生成超时(30秒)提示"文档生成超时，请减少选中的条目后重试" |

---

### 2.2 用户与权限管理

#### 2.2a 账号体系
| 项目 | 描述 |
|------|------|
| **账号创建** | 系统不开放自主注册。管理员登录后在"用户管理"页面创建账号，填写用户名+初始密码，分配给工作人员 |
| **默认管理员** | 系统初始化时自动创建管理员账号（admin），首次登录强制修改密码 |
| **密码策略** | 长度 6~20 位，支持字母数字及符号；密码使用 bcrypt 哈希存储 |
| **认证方式** | JWT Token，登录后发放 access_token（有效期 24 小时）和 refresh_token（有效期 7 天）。access_token 过期后前端自动用 refresh_token 续期，无需用户重复登录 |
| **个人信息** | 每个用户包含：用户名、显示姓名、角色（admin/staff）、创建时间、最后登录时间 |

#### 2.2b 角色权限矩阵

| 功能模块 | 工作人员(staff) | 管理员(admin) |
|----------|:---:|:---:|
| 工作内容上传 | 完全权限 | 完全权限 |
| 浏览与筛选 | 仅看自己上传的 | 查看所有 |
| 编辑工作内容 | 仅自己的 | 所有 |
| 删除工作内容 | 仅自己的 | 所有 |
| 手动修改分类 | 仅自己的 | 所有 |
| 统计页面 | 查看（仅自己数据） | 查看（全部数据） |
| AI 总结生成 | 是（基于自己数据） | 是（基于全部数据） |
| 信息稿生成 | 是 | 是 |
| 分类标签管理 | 不可访问 | 完全权限 |
| 用户管理 | 不可访问 | 完全权限 |

#### 2.2c 登录与鉴权
| 项目 | 描述 |
|------|------|
| **登录页** | 居中登录框：用户名 + 密码 + "登录"按钮。不展示注册入口 |
| **Token 传递** | 所有 API 请求通过 Authorization: Bearer <access_token> 头传递 |
| **鉴权中间件** | Express 全局中间件，白名单路由（/api/auth/login、/api/auth/refresh）以外全部校验 Token |
| **角色中间件** | 敏感路由增加 requireAdmin 中间件，校验 role === 'admin' |
| **401 处理** | Token 无效/过期时返回 401，前端自动跳转登录页并清除 Token |
| **登录限流** | 同一 IP 连续 5 次登录失败后锁定 15 分钟 |

### 2.3 扩展性设计
| 项目 | 描述 |
|------|------|
| **导航架构** | 采用侧边导航栏 + Tab 页签的二级结构<br>- 一级导航：工作流名称（当前仅有"工作内容归档"）<br>- 二级导航：工作流内的功能模块（上传、浏览、统计、信息稿） |
| **扩展方式** | 新增工作流时：<br>1. 在导航栏配置中添加新项<br>2. 创建对应页面组件<br>3. 添加新的路由配置<br>4. 按需扩展数据库表与 API |

---

## 3. 非功能需求

### 3.1 性能要求
| 指标 | 要求 |
|------|------|
| 页面首屏加载 | < 2 秒 |
| 图片上传响应 | < 5 秒（10张图片以内） |
| API 列表查询 | < 500ms（1000条以内） |
| AI 接口调用 | 超时时间 30 秒 |
| Word 文档生成 | < 10 秒 |

### 3.2 安全要求
- API Key 存储在服务端环境变量，不暴露到前端
- 上传文件校验 MIME 类型，拒绝非图片文件
- 上传文件大小限制：单文件 10MB
- SQLite 数据库文件不暴露在 Web 可访问路径

### 3.3 兼容性
- 浏览器：Chrome 90+、Edge 90+
- 操作系统：Windows 10/11
- 推荐分辨率：1920×1080 及以上

### 3.4 数据存储
- 图片原图存储：`server/uploads/originals/YYYY-MM/` 目录，文件名 UUID.扩展名
- 图片缩略图存储：`server/uploads/thumbnails/YYYY-MM/` 目录，原始文件名为 `thumb_UUID.jpg`（统一转 JPEG，宽度 300px）
- 数据库：SQLite 单文件，存放于 `server/data/app.db`
- 需定期备份 `server/data/` 和 `server/uploads/` 目录
- 上传图片时先写磁盘再写数据库，确保数据一致性（数据库写入失败时回滚已写入的文件）

---

## 4. 用户界面草图

### 4.1 整体布局
```
┌──────────────────────────────────────────────────────┐
│  Logo/标题                         [用户头像] [退出]  │
├──────────┬───────────────────────────────────────────┤
│ 导航栏    │  Tab1: 内容上传 │ Tab2: 内容浏览 │ ...    │
│          │                                           │
│ ▸工作内   │  ┌───────────────────────────────────┐   │
│  容归档   │  │                                   │   │
│          │  │       主内容区域                    │   │
│  工作流2  │  │                                   │   │
│  (预留)   │  │                                   │   │
│          │  │                                   │   │
│  工作流3  │  └───────────────────────────────────┘   │
│  (预留)   │                                           │
└──────────┴───────────────────────────────────────────┘
```

### 4.2 内容上传页
```
┌─────────────────────────────────────────────┐
│  工作日期：[ 📅 2026-07-29 ]                 │
│                                              │
│  工作内容说明：                               │
│  ┌───────────────────────────────────────┐   │
│  │  1. 完成XX村道路硬化工程，全长...       │   │
│  │  ─────────────────────────────────    │   │
│  │  2. 组织XX社区环境卫生整治活动...       │   │
│  │  ─────────────────────────────────    │   │
│  │  3. 开展安全生产月检查...              │   │
│  └───────────────────────────────────────┘   │
│                                              │
│  上传图片：                                   │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐       │
│  │ 图1  │ │ 图2  │ │ 图3  │ │ 上传  │       │
│  │      │ │      │ │      │ │   +   │       │
│  └──────┘ └──────┘ └──────┘ └──────┘       │
│                                              │
│  [ 提交归档 ]                                │
└─────────────────────────────────────────────┘
```

### 4.3 统计页面
```
┌─────────────────────────────────────────────┐
│  时间段：[2026-07-01] ~ [2026-07-29]         │
│  分类：  [民生实事 ▾]                        │
│                                              │
│  ┌──────────────────────────────────────┐   │
│  │         柱状图：各分类工作次数          │   │
│  │    ██                                 │   │
│  │    ██ ██                              │   │
│  │ ██ ██ ██                              │   │
│  │ 民生 环境 安全  ...                    │   │
│  └──────────────────────────────────────┘   │
│                                              │
│  统计结果：民生实事 5 次，环境整治 3 次...      │
│                                              │
│  [ 自动生成总结 ]                            │
│  ┌──────────────────────────────────────┐   │
│  │  AI 生成的总结文字...                  │   │
│  │                                       │   │
│  └──────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

---

## 5. 数据库设计

### 5.1 ER 关系
```
users 1──────N work_entries 1──────N work_images
  │                                      │
  │                categories             │
  └────────────────────┘                  │
                                          │
```

### 5.2 建表语句

```sql
-- 用户表
CREATE TABLE users (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  username      TEXT    NOT NULL UNIQUE,
  display_name  TEXT    NOT NULL,
  password_hash TEXT    NOT NULL,
  role          TEXT    NOT NULL DEFAULT 'staff' CHECK(role IN ('admin', 'staff')),
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
  last_login_at DATETIME
);

-- 分类标签表
CREATE TABLE categories (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  name       TEXT NOT NULL UNIQUE,
  sort_order INTEGER DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 工作条目表
CREATE TABLE work_entries (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  batch_id    TEXT    NOT NULL,    -- 批次UUID（同一次提交的多条条目共享）
  description TEXT    NOT NULL,    -- 工作内容描述
  category    TEXT    NOT NULL DEFAULT '未分类',
  work_date   DATE    NOT NULL,    -- 工作发生日期
  created_by  INTEGER NOT NULL,    -- 上传者 users.id
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (created_by) REFERENCES users(id)
);

-- 图片表
CREATE TABLE work_images (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  work_entry_id  INTEGER NOT NULL,
  original_name  TEXT    NOT NULL,   -- 原始文件名
  stored_path    TEXT    NOT NULL,   -- 原图相对存储路径
  thumb_path     TEXT    NOT NULL,   -- 缩略图相对存储路径
  file_size      INTEGER NOT NULL,  -- 文件大小(字节)
  sort_order     INTEGER DEFAULT 0, -- 图片排序
  uploaded_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (work_entry_id) REFERENCES work_entries(id) ON DELETE CASCADE
);

-- 索引
CREATE INDEX idx_work_entries_batch    ON work_entries(batch_id);
CREATE INDEX idx_work_entries_date     ON work_entries(work_date);
CREATE INDEX idx_work_entries_category ON work_entries(category);
CREATE INDEX idx_work_entries_author   ON work_entries(created_by);
CREATE INDEX idx_work_images_entry     ON work_images(work_entry_id);
```

### 5.3 初始数据

```sql
INSERT INTO categories (name, sort_order) VALUES
  ('民生实事', 1),
  ('环境整治', 2),
  ('安全生产', 3);

-- 默认管理员（密码 admin123，首次登录强制修改）
INSERT INTO users (username, display_name, password_hash, role) VALUES
  ('admin', '系统管理员', '$2b$10$...', 'admin');
```

---

## 6. 技术方案概要

| 层级 | 技术选型 | 用途 |
|------|----------|------|
| 前端框架 | React 18 + TypeScript | 类型安全页面构建 |
| UI 组件库 | Ant Design 5 | 布局、表单、表格、日历、上传、分页 |
| 图表 | @ant-design/charts (G2) | 统计图表渲染 |
| 前端构建 | Vite | 开发热更新与生产打包 |
| HTTP 客户端 | axios | API 请求 + 拦截器刷新 Token |
| 后端框架 | Node.js + Express | RESTful API 服务 |
| 数据库 | SQLite (better-sqlite3) | 数据持久化 |
| 文件上传 | multer | multipart/form-data 解析 |
| 图片处理 | sharp | 缩略图生成 |
| AI 接口 | Deepseek API (OpenAI 兼容) | 分类、总结、扩写 |
| Word 生成 | docx (npm) | .docx 文档导出 |
| Excel 生成 | exceljs | 统计报表 .xlsx 导出 |
| 认证 | jsonwebtoken + bcryptjs | JWT 签发/验证 + 密码哈希 |
| UUID | uuid | batch_id、文件名唯一标识 |

---

## 7. 项目里程碑

| 阶段 | 交付物 | 预计时间 |
|------|--------|---------|
| M1 - 基础框架 | 项目脚手架、数据库初始化、登录功能、页面布局 | 第1天 |
| M2 - 上传与分类 | 图片上传、内容录入、AI 分类、分类管理 | 第2-3天 |
| M3 - 浏览与筛选 | 日历筛选、分类筛选、时间段查询、详情展示 | 第4天 |
| M4 - 统计与总结 | 统计图表、AI 总结生成 | 第5-6天 |
| M5 - 信息稿导出 | AI 扩写、Word 文档生成与下载 | 第7天 |
| M6 - 收尾 | 测试、优化、扩展导航预留 | 第8天 |

---

## 8. 风险与依赖

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| Deepseek API 不稳定或限流 | AI 功能不可用 | 设计降级策略：分类降级为"未分类"、总结降级为纯统计数字、扩写降级为使用原文 |
| 图片数量增长导致磁盘不足 | 无法上传 | 预留磁盘空间告警，后续可扩展云存储 |
| SQLite 并发写入瓶颈 | 多用户同时上传变慢 | 当前为单机内部工具，并发需求低，后续可迁移至 PostgreSQL |

---

## 9. 部署方案

### 9.1 开发环境
| 项目 | 配置 |
|------|------|
| 前端开发服务器 | Vite dev server，端口 5173，代理 /api 到 localhost:3001 |
| 后端开发服务器 | nodemon 热重载，端口 3001 |
| 数据库路径 | `server/data/app.db`（开发数据库） |
| 启动命令 | 前端 `npm run dev`，后端 `npm run dev` |

### 9.2 生产环境
| 项目 | 配置 |
|------|------|
| 前端部署 | `npm run build` 产出 `client/dist/`，由 Express 作为静态资源托管 |
| 后端部署 | PM2 进程守护，端口 3001 |
| 数据库路径 | `server/data/app.db`（生产数据库，首次启动自动建表） |
| 环境变量 | `.env` 文件：`DEEPSEEK_API_KEY`、`JWT_SECRET`、`PORT`（默认 3001） |
| 启动命令 | `npm run build && npm start` |
| 反向代理 | 可选 Nginx 前置，提供 HTTPS、静态资源缓存、gzip |

### 9.3 环境变量清单
```
DEEPSEEK_API_KEY=sk-xxxxxxxx     # Deepseek API Key（必填）
DEEPSEEK_BASE_URL=https://api.deepseek.com  # API 地址（可选，默认值）
JWT_SECRET=随机字符串              # JWT 签名密钥（必填，生产环境请用强随机字符串）
PORT=3001                        # 服务端口（可选，默认 3001）
DB_PATH=./server/data/app.db     # 数据库路径（可选，默认值）
UPLOAD_DIR=./server/uploads      # 上传目录（可选，默认值）
```

### 9.4 首次部署步骤
1. `git clone` 项目到目标服务器
2. `npm install`（根目录安装依赖）
3. `cd client && npm install && npm run build`（构建前端）
4. 创建 `.env` 文件，填入 `DEEPSEEK_API_KEY` 和 `JWT_SECRET`
5. `npm start` 启动服务（数据库自动初始化建表 + 创建默认管理员）
6. 浏览器访问 `http://localhost:3001`，使用 `admin / admin123` 登录
7. 首次登录后立即修改管理员密码

### 9.5 运维注意事项
- 定期备份 `server/data/` 和 `server/uploads/` 两个目录
- 图片磁盘用量监控：建议在统计页增加"存储空间"信息展示
- PM2 日志路径配置：`pm2 start server/src/index.js --name party-workbench --log /var/log/party-workbench.log`
- Windows 环境下开发可直接双击 `start.bat` 脚本启动

---

## 10. 修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
|------|------|----------|--------|
| v1.0 | 2026-07-29 | 初始版本 | - |
| v1.1 | 2026-07-29 | 补充：用户认证、权限矩阵、编辑功能、分页搜索、缩略图策略、统计导出、AI成本控制、数据库Schema、部署方案 | - |
