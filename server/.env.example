# Deepseek API Key (也可以在系统设置页面配置)
DEEPSEEK_API_KEY=your-api-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com
JWT_SECRET=change-this-to-a-random-string
PORT=914

# 注意：UPLOAD_DIR 和 DB_PATH 无需配置，系统自动使用 server/uploads 和 server/data/app.db。
# 如需自定义，只能使用绝对路径，相对路径会被忽略。
